{
  "name": "quantum-signer",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "description": "Static client-only post-quantum file signing (ML-DSA / SLH-DSA)",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "selftest": "node scripts/selftest.mjs",
    "check": "npm run selftest && npm run build"
  },
  "dependencies": {
    "@noble/hashes": "2.2.0",
    "@noble/post-quantum": "0.6.1"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
